# CLI 실행 오류 수정 및 이메일 결과 요약표 개편 계획

## 🚨 문제 원인 (Problem Definition)
1. **CLI 자동 실행 즉시 실패 문제:**
   현재 `auto_runner.py`에서 백그라운드 자동 실행을 수행할 때, 브라우저 환경을 준비하는 로직(`capture_captcha_image`)을 생략하고 곧바로 스마트 스킵(`CLICK`) 명령을 전달하여 실행합니다. 브라우저 프로세스가 없는 상태에서 명령이 들어가므로, 세션이나 웹소켓 연결을 찾지 못해 즉시 에러가 발생하며 실패 처리되는 문제가 있습니다.
2. **이메일 결과 요약 가독성 저하:**
   모든 처리가 완료된 후 발송되는 이메일의 하단 '이번 조회 결과' 영역에 사건번호만 쉼표(,)로 나열되어 있습니다. 정보가 부족하고 가독성이 떨어져 피고와 사건명 등 추가 정보를 포함한 가독성 높은 HTML 테이블(표) 형태로 개편이 필요합니다.

## 📝 해결 계획 (Action Plan) - Step 7 구조
**Step 1: `auto_runner.py`의 `MockApp` 클래스 개선**
- `MockApp` 클래스 초기화 시, 브라우저 생성 로직이 내부적으로 기대하는 다중 프로필용 스레드 락 배열 `self.profile_locks`를 추가합니다.
- `config.MAX_PARALLEL_LIMIT` 수에 맞춰 Lock 인스턴스를 생성해 대입합니다.

**Step 2: `services/process_controller.py`에 CLI 전용 처리 메서드 추가**
- `ProcessController` 내부에 `process_cli_auto_case(self, case, case_index)` 메서드를 신규로 구현합니다.
- 해당 메서드에서는 브라우저 및 프로필 기동을 위해 `execute_case_processing_with_captcha`를 먼저 호출하고, 성공적으로 기동된 후 `execute_case_processing`에 `"CLICK"` 파라미터를 넘겨 조회를 수행하도록 구성합니다.

**Step 3: `auto_runner.py`의 메인 루프 로직 변경**
- 백그라운드 자동 실행 루프인 `run_auto_batch()` 내부 워커 함수에서 기존 `_process_auto_case` 호출부를 방금 작성한 `process_cli_auto_case`로 변경하여 브라우저가 정상적으로 실행된 후 스킵 처리 흐름을 타게 수정합니다.

**Step 4: 메일 요약 결과 데이터 구조 변경 - 딕셔너리화 (auto_runner)**
- `auto_runner.py`에서 실행 결과를 누적하는 `success_list`, `fail_list`, `captcha_list`에 단순히 `case_number` 문자열을 삽입하는 대신, `{"사건번호": ..., "피고": ..., "사건명": ...}` 형태의 상세 딕셔너리를 저장하도록 변경합니다.

**Step 5: 메일 요약 결과 데이터 구조 변경 - 딕셔너리화 (process_controller)**
- GUI 기반 모드의 실행 결과를 저장하는 `process_controller.py` 내부의 `_save_run_result_for_email` 메서드 역시, 사건번호 외에 피고와 사건명 정보를 포함한 딕셔너리를 생성하여 넘기도록 데이터를 확장합니다.

**Step 6: 이메일 결과 데이터 호환성 대응 (email_manager)**
- `utils/email_manager.py`의 `set_last_run_result` 및 캐시 로직에서 전달받는 인자가 단순 문자열 리스트가 아닌 딕셔너리 리스트임을 반영해 에러가 발생하지 않고 온전히 캐시에 담기도록 확인 및 수정합니다.

**Step 7: `email_manager.py`의 이메일 하단 표(HTML) 구조 전면 개편**
- `_build_run_result_footer` 함수를 전면 재작성합니다.
- 쉼표로 연결되던 단순 문자열 형태를 제거하고, 성공, 실패, 변경없음, 캡차 등의 각 상태 카테고리별로 `<h4>상태명</h4> <table border="1"><tr><th>사건번호</th><th>피고/사건명</th></tr>...</table>` 형태의 명시적이고 깔끔한 HTML 테이블 구조를 만들어 반환하도록 구현합니다.